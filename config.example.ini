# stt-ptt — Beispielkonfiguration
# Kopiere diese Datei nach ~/.config/stt-ptt/config.ini und passe sie an.

[general]
# Welches Backend genutzt wird: elevenlabs | whisper_remote | whisper_local
backend = whisper_local

# Hotkey (gedrückt halten zum Aufnehmen).
# Beispiele: pause, scroll_lock, f9, right_ctrl, right_alt, menu, insert
hotkey = right_ctrl

# Erzwungene Sprache (ISO 639-1, z.B. "de", "en"). Leer = Auto-Erkennung.
language_code = de

# Audio
sample_rate = 16000
min_recording_seconds = 0.3
max_recording_seconds = 120

# xdotool-Tippverzögerung pro Zeichen (ms). Höher = robuster, langsamer.
type_delay_ms = 8


# ----------------------------------------------------------------------
# Backend 1: ElevenLabs (Cloud, kostenpflichtig, sehr hohe Qualität)
# ----------------------------------------------------------------------
[elevenlabs]
# API-Key von https://elevenlabs.io/app/settings/api-keys
api_key = YOUR_ELEVENLABS_API_KEY
# scribe_v2 (Stand 2026) oder scribe_v1
model_id = scribe_v2


# ----------------------------------------------------------------------
# Backend 2: Whisper auf entferntem (oder lokalem) Server
# OpenAI-kompatibler Endpoint — z.B. faster-whisper-server,
# whisper.cpp Server, LocalAI oder die echte OpenAI-API.
# ----------------------------------------------------------------------
[whisper_remote]
# Basis-URL inkl. /v1 (ohne /audio/transcriptions am Ende)
endpoint = http://localhost:8000/v1
# Modellname (servereigene Konvention)
model = Systran/faster-whisper-base
# api_key = optional, falls Server Auth verlangt
timeout = 60


# ----------------------------------------------------------------------
# Backend 3: Whisper lokal im selben Prozess (faster-whisper)
# Erstinstallation:  ./venv/bin/pip install faster-whisper
# ----------------------------------------------------------------------
[whisper_local]
# tiny | base | small | medium | large-v3  — oder HuggingFace-Repo-ID
model = base
# auto | cpu | cuda
device = auto
# auto | int8 | int8_float16 | float16 | float32
compute_type = auto
